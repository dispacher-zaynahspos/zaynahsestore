cd ~/Desktop/"Zaynahs e-store" && git add . && git commit -m "redeploy fix" && git push origin main

git add .
git commit -m "feat: implement premium fly-to-cart and fly-to-wishlist animations and custom toasts"
git push origin main
